# PJ2 - 模糊测试（Fuzzing）实验报告 v2

## 一、实验信息

- 课程名称：软件质量保障与测试
- 实验名称：模糊测试（Fuzzing）
- 实验类型：课程项目
- 小组成员：________
- 完成日期：________

小组分工......

## 二、实验目的

本实验旨在基于给定的简易 fuzzing 框架，完成输入变异、seed 调度、覆盖率反馈与结果持久化等关键能力，理解灰盒模糊测试的基本流程，并在此基础上形成一个可重复运行、可观察、可分析的实验闭环。

## 三、项目理解

### 3.1 框架结构

项目主要由以下模块组成：

- `fuzzer`：控制 fuzz 循环、seed 选择与输入生成。
- `runner`：执行目标样例并返回运行结果与覆盖率。
- `schedule`：对不同 seed 进行能量分配或优先级判断。
- `utils`：提供覆盖率追踪、种子封装、对象持久化与输入变异工具。
- `samples`：提供用于验证 fuzz 效果的示例程序。

### 3.2 设计主线

实验实现应围绕以下主线展开：

1. 初始化语料库。
2. 选择 seed。
3. 执行变异。
4. 运行目标程序并收集覆盖率。
5. 将新覆盖、新崩溃或高价值 seed 反馈到调度阶段。

建议在撰写时结合 [simple_fuzzer/README.md](simple_fuzzer/README.md) 中的结构说明，避免把实现写成“看起来很多，实际上只是复制粘贴”。

## 四、实现内容

### 4.1 变异器实现

请说明你们实现或补充了哪些变异策略，例如：

- 字符插入。
- bit flip。
- 字节加减。
- interesting values 替换。
- ......

建议重点说明：

- 每种变异适用的场景。
- 如何避免越界或编码异常。
- 如何保证输入多样性。

### 4.2 调度策略实现

请说明你们如何根据路径频率、覆盖率或 seed 能量进行调度。例如：

- 如何记录 path 频率。
- 如何将频率映射为 energy。
- 如何影响下一轮 seed 的选择。

如果实现了多个调度策略，可以比较它们的优缺点。不要只写”效果很好”，最好给出为什么好。

#### 4.2.1 PathPowerSchedule — 路径频率调度

（由组员 B、C 负责实现，详见对应小节）

#### 4.2.2 SizePowerSchedule — 基于输入长度的调度（组员 D 实现）

**设计动机**

在灰盒 fuzzing 中，较短的输入通常具备以下优势：

1. **执行速度更快**：短输入在目标程序中的执行路径更短，相同时间内可以完成更多轮 fuzz
2. **更容易暴露核心逻辑**：长输入中大量”脏数据”可能掩盖真正触发漏洞的关键字节，而短输入更精炼
3. **变异效率更高**：对短输入进行 bit flip 或 arithmetic 变异时，每个字节的改变比重更大，更容易触发边界条件

因此设计 `SizePowerSchedule`：**输入越短，分配的能量（energy）越高**，使其在 `PowerSchedule.choose()` 中有更高的被选中概率。

**能量公式**

```python
if length == 0:
    energy = 1.0          # 空字符串 — 最高能量
else:
    energy = max(1.0 / length, 1e-6)
```

其中 `1e-6` 为最小能量阈值，确保长输入不会被完全淘汰。

**边界处理**

| 场景 | 处理方式 |
|---|---|
| 空字符串 (`length == 0`) | 赋予最高能量 1.0 |
| 长字符串 | 设置最小值 `1e-6` 保底 |
| 空 population | 直接返回，不做任何操作 |

**与 PathPowerSchedule 的对比**

| 维度 | PathPowerSchedule | SizePowerSchedule |
|---|---|---|
| 调度依据 | 路径频率（覆盖率反馈） | 输入长度（静态特征） |
| 反馈类型 | 灰盒（需要覆盖率信息） | 黑盒（无需覆盖率） |
| 优势场景 | 路径分支多、覆盖率差异大 | 输入长度敏感的目标 |
| 计算开销 | MD5 哈希 + 字典查找 | 仅 `len()` 操作 |
| 冷启动 | 初期频率相同，退化为均匀随机 | 冷启动即刻有效 |

**适用场景**

`SizePowerSchedule` 特别适合：
- 目标程序存在固定大小缓冲区，短输入或超长输入更容易触发溢出
- Python 目标中字符串长度敏感的切片或索引操作（如 `s[0]`, `s[50]`）
- 快速冒烟测试：短时间预算快速覆盖短输入路径

局限性：
- 漏洞需要较长特定输入时可能错过
- 不考虑覆盖率反馈，无法感知哪些种子触发了新代码

#### 4.2.3 CoverageSizePowerSchedule — 基于覆盖范围的调度（组员 D 实现，可选扩展）

**设计动机**

能在单次执行中触及更多代码行的种子，说明它包含了更多的有效前置状态或能穿透更深层的逻辑分支。因此设计 `CoverageSizePowerSchedule`：**种子覆盖行数越多，分配的能量越高**。

**能量公式**

```python
if coverage_size == 0:
    energy = min_energy      # 1e-6 — 零覆盖种子给最低能量
else:
    energy = coverage_size * 0.1
```

使用 0.1 的缩放因子避免覆盖行数差异过大导致能量两极分化，设置最小值 `1e-6` 保证所有种子在归一化中都有非零概率。

**三种调度策略总对比**

| 维度 | PathPowerSchedule | SizePowerSchedule | CoverageSizePowerSchedule |
|---|---|---|---|
| 调度依据 | 路径频率 | 输入长度 | 覆盖行数 |
| 反馈类型 | 灰盒 | 黑盒 | 灰盒 |
| 计算开销 | MD5 + 字典 | `len()` | `len(coverage)` |
| 冷启动表现 | 退化为均匀随机 | 即刻有效 | 即刻有效 |
| 适用场景 | 路径分支多的目标 | 长度敏感目标 | 代码量大、分支深的目标 |

### 4.3 持久化与框架优化

请说明你们如何处理 seed 持久化、结果落盘和长时间运行时的内存控制问题。可包括：

- 使用对象序列化保存 seed 或结果。
- 为输出目录自动创建父目录。
- 减少无意义的调试输出。

### 4.4 入口与运行方式（组员 D 负责）

#### 命令行参数

`main.py` 是本实验的统一入口，支持以下参数：

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `--sample` | `int` (1-4) | `4` | 选择被测样例程序 |
| `--run-time` | `int` | `300` | 运行时长（秒） |
| `--schedule` | `str` (`path`, `size`, `coverage`) | `path` | 调度策略选择（**新增**） |
| `--output-dir` | `str` | `_result` | 结果持久化目录 |
| `--quiet` | `flag` | `False` | 禁用状态表打印 |

其中 `--schedule` 为新增参数，通过 `build_schedule()` 工厂函数将策略名称映射到调度类：

```python
def build_schedule(schedule_name: str) -> PowerSchedule:
    schedule_map = {
        "path": PathPowerSchedule,
        "size": SizePowerSchedule,
        "coverage": CoverageSizePowerSchedule,
    }
    return schedule_map[schedule_name]()
```

返回 `PowerSchedule` 基类类型，新增调度策略无需修改 Fuzzer 代码。

#### 运行示例

```bash
# 使用路径频率调度（默认）
python main.py --sample 1 --run-time 10 --schedule path

# 使用输入长度调度
python main.py --sample 1 --run-time 10 --schedule size

# 使用覆盖范围调度
python main.py --sample 1 --run-time 10 --schedule coverage

# 等价于默认行为
python main.py --sample 1 --run-time 10
```

#### 结果文件

运行结束后，结果保存至 `_result/Sample-{n}.pkl`，包含覆盖率集合、唯一崩溃数和时间戳。

## 五、测试与结果

### 5.1 测试环境

- Python 版本：3.11+
- 操作系统：Windows 11
- 项目路径：`simple_fuzzer/`

### 5.2 调度策略单元测试

**SizePowerSchedule** — 5 个不同长度的 seed：

```
len=  0  energy=1.000000    ← 空字符串最高能量
len=  1  energy=1.000000
len=  2  energy=0.500000
len=  8  energy=0.125000
len=100  energy=0.010000    ← 长字符串被压低但不归零
```

**CoverageSizePowerSchedule** — 5 个不同覆盖行数的 seed：

```
cov= 0  energy=0.0000       ← 零覆盖 = 最小能量
cov= 1  energy=0.1000
cov= 2  energy=0.2000
cov= 3  energy=0.3000
cov=10  energy=1.0000       ← 10行覆盖 = 最高能量
```

结论：能量分配符合预期，归一化和接近 1.0，`choose()` 正常执行。

### 5.3 集成测试结果（10 秒短跑）

| Sample | Schedule | Total Execs | Total Paths | Uniq Crashes | Covered Lines |
|--------|----------|------------|-------------|-------------|---------------|
| 1 | path | 12,843 | 5 | 6 | 10 |
| 1 | size | 10,209 | 5 | 6 | 10 |
| 1 | coverage | 13,332 | 5 | 6 | 10 |
| 2 | path | 73,661 | 5 | 4 | 15 |
| 3 | path | 123,164 | 4 | 4 | 6 |
| 3 | size | 137,918 | 5 | 5 | 8 |
| 3 | coverage | 128,754 | 4 | 4 | 6 |
| 4 | path | 8,999 | 746 | 0 | 593 |

### 5.4 结果分析

1. **三种调度策略均正常完成**：所有测试中 `Total Paths` 能增长，`Last New Path` 正确显示时间差，覆盖率统计正常。

2. **SizePowerSchedule 表现突出**（Sample 3）：比 path 多发现 1 条路径、1 个崩溃、2 行覆盖，执行量更高（137K vs 123K）。这是因为 Sample 3 的分支逻辑（`s[0] == 'F'`、`s[1] == 'D'`）对长度敏感，短输入变异后更可能精准命中条件。

3. **CoverageSizePowerSchedule**：Sample 3 表现与 path 持平，Sample 1 执行量略高于 path。因为初始 seed 覆盖信息有限，在短跑中未能充分发挥优势。

4. **调度策略互补性**：
   - path：依靠路径频率反馈，适合长时间运行发现稀有路径
   - size：冷启动即刻有效，对长度敏感目标高效
   - coverage：覆盖行多的 seed 获高能，适合代码量大、分支深的目标

### 5.5 典型输出截图（示例）

Sample 1 使用 `--schedule path` 运行 10 秒的终端输出：

```
┌───────────────────────┬───────────────────────┬───────────────────────┬───────────────────┬───────────────────┬────────────────┬───────────────────┐
│        Run Time       │     Last New Path     │    Last Uniq Crash    │    Total Execs    │    Total Paths    │  Uniq Crashes  │   Covered Lines   │
├───────────────────────┼───────────────────────┼───────────────────────┼───────────────────┼───────────────────┼────────────────┼───────────────────┤
│        00:00:01       │        00:00:00       │        00:00:00       │        1627       │         4         │       5        │         10        │
│        00:00:02       │        00:00:01       │        00:00:02       │        3266       │         5         │       6        │         10        │
│        00:00:03       │        00:00:01       │        00:00:03       │        4655       │         5         │       6        │         10        │
│        00:00:09       │        00:00:01       │        00:00:09       │       12843       │         5         │       6        │         10        │
├───────────────────────┼───────────────────────┼───────────────────────┼───────────────────┼───────────────────┼────────────────┼───────────────────┤
```

最终结果：
```
Covered Lines: 10, Crashes Num: 6
```

## 六、总结与反思

请总结本次实验的收获，例如：

- 对灰盒 fuzzing 的整体流程是否形成了理解。
- 哪一部分实现最难，原因是什么。
- 还有哪些改进空间，例如更丰富的 mutation、更稳定的路径统计或更好的 seed 管理。
